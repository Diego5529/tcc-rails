%h1 Show State

%p
  %b Name:
  = @state.name

%p
  %b Initials:
  = @state.initials

%p
  %b Country:
  = @state.country.name

%p
  = link_to '+ City', new_admin_city_path(@state), class: 'btn btn-info'
%br
= link_to 'Back', admin_states_url