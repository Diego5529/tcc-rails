= simple_form_for [:admin, @event_type], html: {multipart: true} do |f|
  - if @event_type.errors.any?
    %h2
      = pluralize(@event_type.errors.count, 'error')
      prohibited
      this event_type from being saved:
    %ul
      - @event_type.errors.full_messages.each do |m|
        %li= m

  .field
    %p
      %b
        = f.label :title
      %br
      = f.text_field :title

    %p
      %b
        = f.label :short_description
      %br
      = f.text_field :short_description

    %p
      %b
        = f.label :long_description
      %br
      = f.text_field :long_description

    %p
      = f.button :submit, 'Save', class: 'btn btn-primary'